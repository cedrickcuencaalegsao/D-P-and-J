const Button = ({ label }) => {
        return (
          <button className="btn bg-blue-500 text-white rounded-full hover:bg-blue-700 active:bg-blue-800">
  Styled Button
</button>
        );
      };
      
      export default Button;
      